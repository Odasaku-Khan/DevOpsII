apiVersion: v1
kind: Service
metadata:
  name: keycloak-active
  namespace: keycloak
spec:
  selector:
    app.kubernetes.io/name: keycloak
    version: blue          
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP