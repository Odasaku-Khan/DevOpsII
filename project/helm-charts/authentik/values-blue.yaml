replicaCount: 1

image:
  tag: "2025.10.4"

extraLabels:
  version: blue

authentik:
  postgresql:
    host: "postgres-postgresql.postgres.svc.cluster.local"
    port: 5432
    name: "authentik_blue"
    user: "authentik"
    password: "authentik"

worker:
  replicaCount: 1

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 90