replicaCount: 1

image:
  tag: "2025.10.4"

extraLabels:
  version: blue

authentik:
  postgresql:
    host: postgres-postgresql.postgres.svc.cluster.local
    port: "5432"
    user: authentik
    password: authentik
    name: authentik_blue

worker:
  replicaCount: 1

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 90